# Dev / CI dependencies.
pytest>=8.0
ruff>=0.5

# SQL lint (Snowflake dialect — see .sqlfluff). Enforced from the first phase with real SQL
# models (Phase 3), not deferred to Phase 6's CI wiring.
sqlfluff>=3.0
sqlfluff-templater-dbt>=3.0

# Renders docs/lineage.png from dbt's manifest.json (scripts/generate_lineage_graph.py).
# Also needs the `dot` binary: brew install graphviz.
graphviz>=0.20

# dbt: transformation layer. dbt-core + both adapters, so the project builds
# against Snowflake (real warehouse) or DuckDB (no-cloud-account fallback)
# from the same models with just a --target flag. Pins live in dbt-versions.txt,
# shared with orchestration/requirements-airflow.txt -- see that file for why.
-r dbt-versions.txt
