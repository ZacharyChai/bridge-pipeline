# Runtime dependencies. Kept deliberately small — "nothing exotic" (see README stack).
requests>=2.31
psycopg[binary]>=3.1
SQLAlchemy>=2.0
python-dotenv>=1.0

# Snowflake RAW loader (Phase 2). Legacy Postgres path above stays until Snowflake is
# verified end to end — see AUDIT.md and CLAUDE.md's "don't delete the Postgres path
# in one pass" rule.
snowflake-sqlalchemy>=1.6

# REST API (Phase 9) -- read-only endpoints over the dbt marts, queried directly against
# the same DuckDB file dbt already builds. No new warehouse client: duckdb's own Python
# driver, same "nothing exotic" philosophy as the rest of this file.
fastapi>=0.115
uvicorn[standard]>=0.32
duckdb>=1.1
